import React, {FC} from 'react';
import {View, StyleSheet, Text} from 'react-native';
import {Formik} from 'formik';

interface Props {
  answer: any;
}

const PhoneEdit: FC<Props> = props => {
    return (
        <View>
            <Text></Text>
        </View>
    )
};

const styles = StyleSheet.create({});

export default PhoneEdit;
